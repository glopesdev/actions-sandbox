name: CI

on:
    push:
    pull_request:

jobs:
    build:
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os: [ubuntu-latest, windows-latest]
            fail-fast: false
        steps:
            - uses: actions/checkout@v4
            
            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: '8.x'
            
            - name: Restore tools
              run: dotnet tool restore
            
            - name: Print tools available
              run: dotnet tool list --local
            
            - name: Print version
              run: dotnet tool run bonsai.sgen --version
            - name: Run bonsai.sgen
              run: dotnet tool run bonsai.sgen schema.json